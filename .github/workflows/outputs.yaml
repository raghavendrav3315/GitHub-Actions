name: output demo
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: step one
        id: step1
        run: echo "version=1.15.8" >> $GITHUB_OUTPUT
        
      - name: step two
        run: echo "this is version ${{ steps.step1.outputs.version }}"
      
